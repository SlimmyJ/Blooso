<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="Blooso.Views.MainMenuPage"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Blooso.ViewModels;assembly=Blooso">
    <ContentPage.BindingContext>
        <viewModels:MainMenuViewModel />
    </ContentPage.BindingContext>
    <RefreshView>
        <StackLayout BackgroundColor="{StaticResource BaseOrange}">
            <Button Command="{Binding LoadUsersCommand}"
                    Margin="0,20"
                    HorizontalOptions="Center"
                    Text="Find matches!"
                    VerticalOptions="Center" />
        </StackLayout>
    </RefreshView>
</ContentPage>