<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Blooso.Views.EditProfile.EditUserTagsListPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModels="clr-namespace:Blooso.ViewModels;assembly=Blooso">
    <ContentPage.BindingContext>
        <viewModels:EditUserTagsListViewModel />
    </ContentPage.BindingContext>
    <StackLayout>
<<<<<<< HEAD
        <StackLayout Padding="10,20" BackgroundColor="#ededed">
            <Label FontAttributes="None" Text="Tags let us match you up with people that have the same interests. Add more to your profile to make better connections." />
=======
        <StackLayout BackgroundColor="#ededed"
                     Padding="10,20">
            <Label Text="Tags let us match you up with people that have the same interests. Add more to your profile to make better connections."
                   FontAttributes="None"
                   TextColor="#a5a2a2"/>
>>>>>>> origin/master
        </StackLayout>
        <!--  Users chosen tags  -->
        <StackLayout Margin="0,40,0,0">
<<<<<<< HEAD
            <Label
                Padding="20,5"
                BackgroundColor="#ededed"
                FontAttributes="Italic"
                HorizontalOptions="Center"
                Text="Your favorite tags" />
            <FlexLayout
                Padding="10"
                BindableLayout.ItemsSource="{Binding UserTags}"
                JustifyContent="SpaceEvenly"
                Wrap="Wrap">
                <BindableLayout.ItemTemplate>
                    <DataTemplate>
                        <Frame
                            Padding="10"
                            BorderColor="LightSlateGray"
                            CornerRadius="25">
                            <Label FontSize="12" Text="{Binding}">
=======
            <Label Text="Your favorite tags"
                   HorizontalOptions="Center"
                   Padding="20,5"
                   FontAttributes="Italic"
                   BackgroundColor="#ededed"
                   TextColor="#a5a2a2"/>
            <FlexLayout BindableLayout.ItemsSource="{Binding UserTags}"
                        Wrap="Wrap"
                        JustifyContent="SpaceEvenly"
                        Padding="10">
                <BindableLayout.ItemTemplate>
                    <DataTemplate>
                        <Frame BorderColor="LightSlateGray"
                               CornerRadius="25"
                               Padding="10">
                            <Label Text="{Binding}"
                                   FontSize="12"
                                   TextColor="#a5a2a2">
>>>>>>> origin/master
                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:EditUserTagsListViewModel}}, Path=DeleteTagFromListCommand}"
                                        CommandParameter="{Binding .}"
                                        NumberOfTapsRequired="1" />
                                </Label.GestureRecognizers>
                            </Label>
                        </Frame>
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </FlexLayout>
        </StackLayout>
<<<<<<< HEAD
        <!--  All tags  -->
        <StackLayout Margin="0,0,0,80" VerticalOptions="EndAndExpand">
            <Label
                Padding="20,5"
                BackgroundColor="#ededed"
                FontAttributes="Italic"
                HorizontalOptions="Center"
                Text="All tags" />
            <FlexLayout
                Padding="10"
                BindableLayout.ItemsSource="{Binding Tags}"
                JustifyContent="SpaceEvenly"
                Wrap="Wrap">
                <BindableLayout.ItemTemplate>
                    <DataTemplate>
                        <Frame
                            Padding="10"
                            BorderColor="LightSlateGray"
                            CornerRadius="25">
                            <Label FontSize="12" Text="{Binding}">
=======

        <!--All tags-->
        <StackLayout VerticalOptions="EndAndExpand"
                     Margin="0,0,0,80">
            <Label Text="All tags"
                   HorizontalOptions="Center"
                   Padding="20,5"
                   FontAttributes="Italic"
                   BackgroundColor="#ededed"
                   TextColor="#a5a2a2"/>
            <FlexLayout BindableLayout.ItemsSource="{Binding Tags}"
                        Wrap="Wrap"
                        JustifyContent="SpaceEvenly"
                        Padding="10">
                <BindableLayout.ItemTemplate>
                    <DataTemplate>
                        <Frame BorderColor="LightSlateGray"
                               CornerRadius="25"
                               Padding="10">
                            <Label Text="{Binding}"
                                   FontSize="12"
                                   TextColor="#a5a2a2">
>>>>>>> origin/master
                                <Label.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:EditUserTagsListViewModel}}, Path=AddToTagsListCommand}"
                                        CommandParameter="{Binding .}"
                                        NumberOfTapsRequired="1" />
                                </Label.GestureRecognizers>
                            </Label>
                        </Frame>
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </FlexLayout>
        </StackLayout>
    </StackLayout>
</ContentPage>