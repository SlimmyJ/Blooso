<Shell x:Class="Blooso.AppShell"
       xmlns="http://xamarin.com/schemas/2014/forms"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:Blooso.Views;assembly=Blooso"
       Title="Blooso"
       BackgroundColor="#FFFF8C42">
    <Shell.Resources>
        <ResourceDictionary />
    </Shell.Resources>
    <TabBar>
        <ShellContent Title="Profile" Icon="icon_about.png" Route="AboutPage" ContentTemplate="{DataTemplate local:MainMenuPage}" />
        <ShellContent Title="Friendlist" Icon="icon_feed.png" ContentTemplate="{DataTemplate local:FriendlistPage}" />
        <ShellContent Title="Messages" Icon="icon_feed.png" ContentTemplate="{DataTemplate local:MatchOverviewPage}" />
    </TabBar>
    <!--
        If you would like to navigate to this content you can do so by calling
        await Shell.Current.GoToAsync("//LoginPage");
    -->
    <TabBar>
        <ShellContent Route="MainMenuPage"
                      ContentTemplate="{DataTemplate local:MainMenuPage}" />
    </TabBar>
    <!--<FlyoutItem Title="MainMenu">
        <ShellContent ContentTemplate="{DataTemplate local:MainMenuPage}"
                      Route="MainMenuPage" />
    </FlyoutItem>
    <TabBar>
        <ShellContent Route="MainMenuPage" ContentTemplate="{DataTemplate local:MainMenuPage}" />
    </TabBar>-->
</Shell>